% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_readers.R
\name{read_own_data}
\alias{read_own_data}
\title{Read an instrument export file (own LPSA data)}
\usage{
read_own_data(path)
}
\arguments{
\item{path}{Character. Path to the tab-delimited instrument export file.}
}
\value{
A list with two tibbles:
\describe{
\item{psd}{Long-format: one row per sample per grain-size bin.
Columns: \code{sample_name}, \code{sample_id}, \code{depth_val}, \code{meta_code},
\code{rep_num}, \code{note}, \code{grain_um}, \code{perc_vol_in}.}
\item{summary}{Long-format: one row per sample per summary metric
(e.g. \code{dx_10}, \code{dx_50}, \code{dx_90}). Columns: \code{sample_name},
\code{sample_id}, \code{depth_val}, \code{meta_code}, \code{note}, \code{metric}, \code{value_raw}.}
}
}
\description{
Parses a tab-delimited Mastersizer export, keeping only the
\code{"Average of '...'"} rows, and splits it into per-bin particle size
distribution data and per-sample summary statistics. Sample names are
parsed according to the package's naming convention
(\verb{<sample_id>_<meta_code>_<rep_number>_<optional_note>}, see \code{CLAUDE.md}).
}
\examples{
\dontrun{
own <- read_own_data("data/run1.txt")
own$psd
own$summary
}
}
